The current Sales screen is functionally correct but feels like a basic form instead of a guided SaaS flow. I want you to refactor ONLY the SalesScreen.tsx UI/UX to make it more guided and self‑explanatory, without breaking any existing logic, navigation, or state store.

Please follow these constraints and improvements:

1) Do NOT change logic, types, or navigation targets
Keep all existing imports, including RootStackParamList and the selection store (saleSelectionStore.ts).

Keep using the same selectedCustomer / selectedProduct state and resetSaleSelection, getSelectedCustomerState, getSelectedProductState, etc.

Do not rename any routes: CustomerSelect, ProductSelect, ReviewOrder, Home must stay the same.

Do not change how selections are stored or read – only the presentation layer (JSX/layout/copy).

If you need extra small helpers (e.g., booleans like hasCustomer), do them inline in the component, not in the store.

2) Add a clear, 3‑step guided flow
Convert the top of the screen into a guided flow:

Keep the main title “New Sale”.

Under it, show a horizontal or vertical stepper with three steps:

Select customer

Select product

Review & complete

Behavior:

Step 1 should visually indicate “completed” when a customer is selected, otherwise “pending”.

Step 2 same for product.

Step 3 should appear “ready” only when both 1 and 2 are completed.

You can implement this with simple React Native views and text (no 3rd‑party libraries).

3) Improve Customer & Product sections with progress/state
For each section:

Empty state:

Show a subtle card with helper copy, e.g. for Customer:
“No customer selected. Tap ‘Select customer’ to choose”.

For Product:
“No product selected. Tap ‘Select product’ to choose”.

Selected state:

Keep showing the existing card with name/phone or product/price/stock.

Add a small “✓ Selected” indicator or pill so it feels like progress.

Keep or slightly restyle the “Change” / “Select” action, but make the intent explicit:

“Select customer” when none is selected.

“Change customer” when already selected.

Same for product.

Don’t change how customer/product are loaded or stored; only adjust copy/layout/styles.

4) Make the primary path obvious (Review Order)
“Review Order” is the primary action. Keep it as the main green button, but:

Disable it (visually and functionally) until both a customer and product are selected.

When disabled, show subtle helper text under it like:

“Select a customer and product to continue.”

When both are selected, enable the button and (optionally) change the text to something that indicates progression, e.g. “Step 3: Review order”.

Do NOT change the navigation target – it must still navigate to ReviewOrder using the existing function (just wrap the call in an eligibility check if needed).

5) Improve Reset & Main Menu actions
Keep both actions but make them secondary and safe:

“Reset”:

Should clear the current selection using existing logic: call resetSaleSelection() and update local state exactly as today.

Before clearing, if there is any selected customer or product, show a confirmation alert:
Title: “Clear current sale?”
Message: “This will remove the selected customer and product.”
Buttons: Cancel / Clear.

“Main Menu”:

Keep navigation to Home as is.

Rename label from “Main Menu” to “Back to Dashboard” for clarity, but keep route name unchanged.

Visual styling:

Place “Review Order” as the primary button.

Place “Reset” and “Back to Dashboard” below it, using a lighter/outline style so they clearly look secondary.

6) Add inline helper/error messaging, not just alerts
If the user somehow taps “Review Order” while customer or product is missing (in case the button gets enabled incorrectly), show a short inline helper text near the relevant section instead of only an alert.

Example: under Product section, a red helper line: “Select a product to continue”.

You can keep existing alerts for validation but add these inline hints for guidance.

Implement this with simple conditional rendering based on !selectedCustomer / !selectedProduct.

7) Add a basic sale summary placeholder
Even if the app currently supports only a single product and default quantity:

Under the Product section, add a small “Sale summary” card that shows:

“Customer: [customer name]” (or “Not selected”).

“Product: [product name]” (or “Not selected”).

“Quantity: 1” (hardcoded for now, but visually indicates future capability).

“Estimated total: [product.price]” (if a product is selected).

This is purely presentational: do NOT change or add any state for quantity or total; just derive it from current product info.

8) Do not change any external files or services
Keep saleSelectionStore.ts unchanged.

Do not modify navigation configuration (AppNavigator.tsx or App.tsx).

Do not modify services (customerService, productService, etc.).

All changes must stay fully inside SalesScreen.tsx (except for any local StyleSheet updates within that file).

9) Return the full, updated SalesScreen.tsx
Provide a complete, ready‑to‑paste SalesScreen.tsx file that:

Compiles with current project typings.

Keeps all existing imports and adds new ones only if needed for styling (e.g., icons from react-native only, no new libraries).

Uses only React Native core components and existing navigation/hooks.

Remember: the goal is to turn this into a guided “New Sale” builder without breaking any existing behavior. Keep the underlying logic and routes identical; only improve structure, copy, and UX flow