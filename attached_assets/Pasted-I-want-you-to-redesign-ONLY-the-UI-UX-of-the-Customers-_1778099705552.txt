<<<<<<< HEAD
I want you to redesign ONLY the UI/UX of the Customers screen to match a modern SaaS-style mobile app, consistent with my current Sales screen flow. Keep ALL existing behavior intact.

This screen is implemented in src/screens/CustomersScreen.tsx and already:

Fetches customers via customerService (e.g., getCustomers).

Uses React Navigation with RootStackParamList from ../navigation/AppNavigator.

Has working navigation to other screens (e.g., AddCustomer, Sales, etc.).

You must treat the current logic as correct and untouchable.

🚫 DO NOT CHANGE (VERY IMPORTANT)

DO NOT break or modify any logic, APIs, hooks, or navigation behavior.

DO NOT remove or change data fetching (e.g., getCustomers call).

DO NOT change function names, their arguments, or when they are called.

DO NOT change route names or navigation targets.

DO NOT alter TypeScript types or imports from services / navigation.

DO NOT introduce new dependencies (no new libs).

KEEP all existing data bindings; just rearrange and restyle the JSX.

KEEP the same click behavior: Add Customer, tap on a customer, etc.

All changes must be limited to:

Layout

Styling

Component structure (within this file)

Text/copy for labels and helper text

🎨 DESIGN SYSTEM (must match Sales screen)

Use this visual system, consistent with the Sales screen:

Screen background: #F8FAFC

Card background: #FFFFFF

Border: 1px solid #E2E8F0

Primary color: #2563EB

Accent/positive color (if needed): #16A34A

Text primary: #0F172A

Text secondary: #64748B

Border radius: 12–16

Spacing scale: 8, 12, 16, 20

REMOVE heavy shadows

Use minimal, consistent colors (no random colors)

Use StyleSheet.create instead of heavy inline styles.

🧱 REQUIRED UI / FLOW CHANGES

HEADER

Clean, left-aligned title: “Customers”.

Optionally, a short subtitle like:
“Manage customers and quickly access them for new sales.”

No banners or large hero blocks; keep it simple like the “New Sale” header.

ADD A SEARCH BAR (very important)

Place directly under the header.

Use a simple TextInput styled as a search bar.

Placeholder: “Search customers by name or phone”.

Filter the existing in-memory customers list based on this search term:

Match on name or phone (case-insensitive).

Do NOT change how customers are fetched from the service.

Keep the original array intact; filter only for rendering.

If search has no matches, show a friendly “No customers found” message.

ACTION BUTTONS (clear hierarchy)

Add a small actions block below the search bar:

Primary button:

Label: “+ Add Customer”

Uses primary color #2563EB.

Full width or at least visually dominant.

Must still trigger the existing logic that navigates to AddCustomer.

Secondary button:

Label: “Import / Update from CSV”

Use a subtle, outlined or neutral style (e.g., border #E2E8F0, text #0F172A).

Hook it to the existing (or placeholder) handler if present; if there isn’t one, just keep it as a visual button with a TODO comment.

It must NOT break current behavior—if no logic exists, keep it non-destructive.

Ensure the primary button is visually emphasized more than the secondary one, similar to how “Review Order” is primary on the Sales screen.

CUSTOMER LIST – redesign each item as a card

For each customer in the (filtered) list, redesign the row into a card with this structure:

Container:

Background: white (#FFFFFF).

Border: #E2E8F0.

Border radius: ~12.

Padding: 14–16.

Margin bottom: 10–12.

LEFT: Circular avatar with initials

Example: “Munawar Khursheed” → “MK”.

Use a simple View with a circle style and text inside; no avatar images.

Background can be a light neutral (e.g., #E5E7EB), text primary color.

CENTER: Text block

Line 1: Customer name, bold, 14–16, primary text color.

Line 2: Phone and city, smaller and muted, e.g.:
0307xxxxxxx • Karachi
(If city is missing, just show phone.)

Ensure text wraps gracefully on smaller screens.

RIGHT: Clear action affordance

An “Edit” text button or chip.

Use a small, subtle primary color text (#2563EB), not a huge primary button.

It should be visually clear but not overpower the card content.

Hook this to the existing logic you currently have when tapping a customer or editing (if any). If tapping the whole card already navigates to a details/edit flow, keep that behavior; the “Edit” button can call the same handler.

Example visual layout:

[ MK] Munawar Khursheed
0307xxxxxxx - Karachi Edit

Do all of this within the existing map over customers (or filtered list), without changing the actual data.

LIST STYLING & EMPTY STATES

If there are no customers:

Show a friendly, centered message:

“No customers yet.”

“Tap ‘Add Customer’ to create your first one.”

Use secondary text color.

Improve spacing:

Add a bit of vertical spacing between cards.

Ensure the list scrolls smoothly and feels like a modern SaaS list, not a plain flat list.

OPTIONAL MICRO-INTERACTIONS (ONLY IF EASY, no logic changes)

If you can implement this without changing service or navigation logic:

Make the phone number tappable:

Use Linking.openURL('tel:' + phone) on press.

Wrap that in a small pressable; do not change the core card tap behavior.

Optionally add tiny icons for call or WhatsApp on the right side:

Use simple text or Unicode icons (no new SVG/icon library).

Do NOT change the existing onPress behavior of the whole card; these should be additional, not replacing anything.

If adding this would conflict with current handlers, skip it to avoid logic changes.

🧩 COMPONENT ORGANIZATION (within this file only)

If it makes the code cleaner, you may define small internal components in this file:

CustomerListItem

PrimaryButton

SearchBar

But do NOT move them to separate files; keep everything inside CustomersScreen.tsx.

Avoid large inline style objects; use StyleSheet.create clearly.

⚠️ DO NOT

Do NOT change Firestore / API calls.

Do NOT modify customerService or any other service file.

Do NOT touch navigation configuration or route names.

Do NOT hardcode sample customers into the component.

Do NOT introduce new dependencies or heavy styling libraries.

Do NOT add bright/random colors or shadows.

🎯 FINAL GOAL

Transform this screen from:

“Basic list of customers with a placeholder look”

into:

“Clean, guided SaaS customer management screen aligned with the Sales ‘New Sale’ flow”

Focus on:

Clear hierarchy: title → search → actions → list.

Fast scanning of customers.

Visual polish that matches the Sales screen’s design system.

Zero behavior regressions.

=======
I want you to redesign ONLY the UI/UX of the Customers screen to match a modern SaaS-style mobile app, consistent with my current Sales screen flow. Keep ALL existing behavior intact.

This screen is implemented in src/screens/CustomersScreen.tsx and already:

Fetches customers via customerService (e.g., getCustomers).

Uses React Navigation with RootStackParamList from ../navigation/AppNavigator.

Has working navigation to other screens (e.g., AddCustomer, Sales, etc.).

You must treat the current logic as correct and untouchable.

🚫 DO NOT CHANGE (VERY IMPORTANT)

DO NOT break or modify any logic, APIs, hooks, or navigation behavior.

DO NOT remove or change data fetching (e.g., getCustomers call).

DO NOT change function names, their arguments, or when they are called.

DO NOT change route names or navigation targets.

DO NOT alter TypeScript types or imports from services / navigation.

DO NOT introduce new dependencies (no new libs).

KEEP all existing data bindings; just rearrange and restyle the JSX.

KEEP the same click behavior: Add Customer, tap on a customer, etc.

All changes must be limited to:

Layout

Styling

Component structure (within this file)

Text/copy for labels and helper text

🎨 DESIGN SYSTEM (must match Sales screen)

Use this visual system, consistent with the Sales screen:

Screen background: #F8FAFC

Card background: #FFFFFF

Border: 1px solid #E2E8F0

Primary color: #2563EB

Accent/positive color (if needed): #16A34A

Text primary: #0F172A

Text secondary: #64748B

Border radius: 12–16

Spacing scale: 8, 12, 16, 20

REMOVE heavy shadows

Use minimal, consistent colors (no random colors)

Use StyleSheet.create instead of heavy inline styles.

🧱 REQUIRED UI / FLOW CHANGES

HEADER

Clean, left-aligned title: “Customers”.

Optionally, a short subtitle like:
“Manage customers and quickly access them for new sales.”

No banners or large hero blocks; keep it simple like the “New Sale” header.

ADD A SEARCH BAR (very important)

Place directly under the header.

Use a simple TextInput styled as a search bar.

Placeholder: “Search customers by name or phone”.

Filter the existing in-memory customers list based on this search term:

Match on name or phone (case-insensitive).

Do NOT change how customers are fetched from the service.

Keep the original array intact; filter only for rendering.

If search has no matches, show a friendly “No customers found” message.

ACTION BUTTONS (clear hierarchy)

Add a small actions block below the search bar:

Primary button:

Label: “+ Add Customer”

Uses primary color #2563EB.

Full width or at least visually dominant.

Must still trigger the existing logic that navigates to AddCustomer.

Secondary button:

Label: “Import / Update from CSV”

Use a subtle, outlined or neutral style (e.g., border #E2E8F0, text #0F172A).

Hook it to the existing (or placeholder) handler if present; if there isn’t one, just keep it as a visual button with a TODO comment.

It must NOT break current behavior—if no logic exists, keep it non-destructive.

Ensure the primary button is visually emphasized more than the secondary one, similar to how “Review Order” is primary on the Sales screen.

CUSTOMER LIST – redesign each item as a card

For each customer in the (filtered) list, redesign the row into a card with this structure:

Container:

Background: white (#FFFFFF).

Border: #E2E8F0.

Border radius: ~12.

Padding: 14–16.

Margin bottom: 10–12.

LEFT: Circular avatar with initials

Example: “Munawar Khursheed” → “MK”.

Use a simple View with a circle style and text inside; no avatar images.

Background can be a light neutral (e.g., #E5E7EB), text primary color.

CENTER: Text block

Line 1: Customer name, bold, 14–16, primary text color.

Line 2: Phone and city, smaller and muted, e.g.:
0307xxxxxxx • Karachi
(If city is missing, just show phone.)

Ensure text wraps gracefully on smaller screens.

RIGHT: Clear action affordance

An “Edit” text button or chip.

Use a small, subtle primary color text (#2563EB), not a huge primary button.

It should be visually clear but not overpower the card content.

Hook this to the existing logic you currently have when tapping a customer or editing (if any). If tapping the whole card already navigates to a details/edit flow, keep that behavior; the “Edit” button can call the same handler.

Example visual layout:

[ MK] Munawar Khursheed
0307xxxxxxx - Karachi Edit

Do all of this within the existing map over customers (or filtered list), without changing the actual data.

LIST STYLING & EMPTY STATES

If there are no customers:

Show a friendly, centered message:

“No customers yet.”

“Tap ‘Add Customer’ to create your first one.”

Use secondary text color.

Improve spacing:

Add a bit of vertical spacing between cards.

Ensure the list scrolls smoothly and feels like a modern SaaS list, not a plain flat list.

OPTIONAL MICRO-INTERACTIONS (ONLY IF EASY, no logic changes)

If you can implement this without changing service or navigation logic:

Make the phone number tappable:

Use Linking.openURL('tel:' + phone) on press.

Wrap that in a small pressable; do not change the core card tap behavior.

Optionally add tiny icons for call or WhatsApp on the right side:

Use simple text or Unicode icons (no new SVG/icon library).

Do NOT change the existing onPress behavior of the whole card; these should be additional, not replacing anything.

If adding this would conflict with current handlers, skip it to avoid logic changes.

🧩 COMPONENT ORGANIZATION (within this file only)

If it makes the code cleaner, you may define small internal components in this file:

CustomerListItem

PrimaryButton

SearchBar

But do NOT move them to separate files; keep everything inside CustomersScreen.tsx.

Avoid large inline style objects; use StyleSheet.create clearly.

⚠️ DO NOT

Do NOT change Firestore / API calls.

Do NOT modify customerService or any other service file.

Do NOT touch navigation configuration or route names.

Do NOT hardcode sample customers into the component.

Do NOT introduce new dependencies or heavy styling libraries.

Do NOT add bright/random colors or shadows.

🎯 FINAL GOAL

Transform this screen from:

“Basic list of customers with a placeholder look”

into:

“Clean, guided SaaS customer management screen aligned with the Sales ‘New Sale’ flow”

Focus on:

Clear hierarchy: title → search → actions → list.

Fast scanning of customers.

Visual polish that matches the Sales screen’s design system.

Zero behavior regressions.

>>>>>>> 8f32440 (Initial app update)
Finally, return the full, updated CustomersScreen.tsx file, ready to paste, that compiles against the existing project.